/* Pure vehicle-tax ↔ Finance integrity checks. */
(function(g){'use strict';function reconcile(input){const vehicles=Array.isArray(input?.vehicles)?input.vehicles:[],txs=Array.isArray(input?.transactions)?input.transactions:[],ids=new Set(vehicles.filter(Boolean).map(v=>v.id)),issues=[];txs.forEach(t=>{const link=t?.vehicleTaxLink;if(!link)return;if(!link.vehicleId||!ids.has(link.vehicleId))issues.push({code:'MISSING_TAX_VEHICLE',vehicleId:link.vehicleId||null,transactionId:t.id});if(!['annual','five_year'].includes(link.taxType))issues.push({code:'INVALID_TAX_TYPE',transactionId:t.id,taxType:link.taxType});if(!(Number(t.amount)>0))issues.push({code:'INVALID_TAX_AMOUNT',transactionId:t.id});if(t.date&&Number.isNaN(new Date(t.date).getTime()))issues.push({code:'INVALID_TAX_DATE',transactionId:t.id});});return{ok:!issues.length,issues};}const api={reconcile};if(typeof window!=='undefined')window.VehicleTaxIntegrityReconciler=api;if(typeof globalThis!=='undefined')globalThis.VehicleTaxIntegrityReconciler=api;if(typeof module!=='undefined')module.exports=api;})(typeof globalThis!=='undefined'?globalThis:this);
