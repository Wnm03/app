const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');
function load(){const sandbox={window:{},console,D:{vehicles:[]},VehicleCatalog:null,VehiclePartSOT:{seed:[{category:'Mesin',subcategory:'Cylinder',partName:'Busi'},{category:'CVT',subcategory:'Pulley',partName:'V-Belt'}]},DatabaseAPI:{vehicle:{modelGetAll:()=>[{id:'vario-125',manufacturerId:'honda',name:'Honda Vario 125 (KZR)',matchNames:['vario 125'],torsi:{cats:[{cat:'Fallback',items:[{name:'X'}]}]}}]}}};vm.createContext(sandbox);for(const f of ['modules/vehicle/vehicle-model-registry-sot.js','modules/vehicle/vehicle-sot-provisioning.js'])vm.runInContext(fs.readFileSync(f,'utf8'),sandbox);return sandbox.window.VehicleSOTProvisioning;}
test('registration exposes vehicle type and model taxonomy',async()=>{const api=load();const v={id:'veh_x',name:'Honda Vario Techno 125 KZR',jenis:'motor'};await api.provisionVehicle(v);assert.equal(v.modelId,'vario-125');assert.equal(v.vehicleType,'motor');assert.equal(v.bodyType,'matic');assert.equal(v.modelGeneration,'KZR');assert.ok(v.sot.taxonomy.some(x=>x.name==='Mesin'));assert.equal(v.sot.componentCount,2);});
