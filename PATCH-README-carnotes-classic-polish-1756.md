# Car Notes Classic Polish 2.0 — cumulative patch (baseline app-main 18)

Presentation-focused refinement scoped to `#page-carnotes`. Preserves existing Car Notes business logic, storage schema, navigation contracts, Servis/BBM/fuel/finance/stock linkage, and the Theme Pro rollback.

Includes compact vehicle context, stronger tab hierarchy, denser cards/stat blocks, mobile touch targets, safe-area spacing, responsive layout, reduced-motion support, focus-visible accessibility, long-list overflow protection, primary CTA polish, cache-busting 1756, and regression tests.

Patch is cumulative relative to `app-main (18).zip`; it contains only files changed/new relative to that baseline plus the deletion manifest.
