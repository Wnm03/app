PATCH-ONLY FINAL — app-main (11) baseline

BASELINE: app-main (11).zip
BASELINE SHA-256: 1d0759a09b479a04dd2954a619cb298909c4a7d236d66e0921c2230a7709aaa8
TARGET BUILD: sesi-servis-actiontype-riwayat-leak-merge-1708

This archive contains ONLY files whose bytes differ from the clean app-main (11)
baseline after:
  1) applying patch app-main11-servis-final-1707-PATCH-ONLY (servis
     split/reconciliation fix + regression-test updates), and
  2) running `node scripts/build.js` on top of that (version bump
     1707 -> 1708, bundle regeneration, HTML/sw.js cache-bust sync).

It is NOT a full release/source archive. It supersedes and includes the
contents of app-main11-servis-final-1707-PATCH-ONLY (that patch's manifest/
audit files are folded into this one; no separate 1707 patch files are
included here to avoid ambiguity about which manifest is authoritative).

Structural final gate (carried over from 1707, re-verified after build):
- const Servis declaration: exactly 1, modules/vehicle/servis.js
- car-notes.js: no const Servis declaration / no window.Servis owner
- scripts/build.js: modules/vehicle/servis.js present in GROUP_B
- cnPeriodeByTab: restored and synchronized

Build step (this patch, on top of 1707):
- node scripts/build.js run successfully
- version synced across: modules/shared/modules-render.js,
  modules/shared/modals.js, modules/shared/modules-calc.js,
  chat-action-handlers.js, modules/shared/features-helpers-global-security.js
- app-bundle-a.min.js / app-bundle-b.min.js regenerated (UNMINIFIED —
  esbuild not available in the build environment; bundles are syntactically
  valid, `node --check` passes on both, just larger than a minified release)
- app_production.html, index.html: all ?v= bumped to 1708
- sw.js: CACHE_NAME bumped to kw-cache-v1708
- docs/FILE-MAP.md, docs/COVERAGE-PER-MODULE.md regenerated by build

Test inventory: full suite executed after build.
  node --test tests/*.test.js
  762 test files -> 6767 individual test cases
  RESULT: 6767 pass / 0 fail / 0 cancelled / 0 skipped

NOTE ON ESBUILD: to get a minified bundle matching prior release sizes, run
`npm install --save-dev esbuild` in an environment with internet access,
then re-run `node scripts/build.js`. Not done here (sandboxed, no network).

IMPORTANT: apply this ZIP over a clean app-main (11) tree. Do not treat it as
a standalone release ZIP. This patch already contains everything from the
1707 patch plus the 1708 build changes — do not layer it on top of the 1707
patch zip, apply it directly on the clean baseline instead.
